import React from 'react';
import styled from 'styled-components';
import { ItemType } from 'types';

interface Props {
  results: ItemType[];
}

const AskPresenter = ({ results }: Props) => {
  return <div>123</div>;
};

export default AskPresenter;
